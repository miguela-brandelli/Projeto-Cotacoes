FROM node:18-alpine

WORKDIR /app

# Copia package.json
COPY package*.json ./

# Instala dependências
RUN npm ci

# Expõe porta do Vite
EXPOSE 5173

# Roda em modo desenvolvimento com hot reload
CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0"]